export type EventKeys =
    | `startConversation`
    | `breakConversation`
    | `pauseConversation`
    | `resumeConversation`;

export type EventTypes =
    | `start-conversation`
    | `break-conversation`
    | `pause-conversation`
    | `resume-conversation`;